# Shell stuff
alias reload="exec $SHELL"
alias ..="cd .."                      
alias ...="cd ../.." 

# NeoVim
alias n="nvim"

# tmux
alias tl="tmux ls"
alias ta="tmux attach -t"
alias tn="tmux new -s"

# Git
alias ga="git add ."
alias gs="git status"
alias gc="git commit -m"
